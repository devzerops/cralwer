version: '3.8'

services:
  server:
    build: ./cmd/server
    ports:
      - "8080:8080"
    environment:
      - NODE_ENV=production

  worker:
    build: ./cmd/worker
    ports:
      - "8081:8081"
    environment:
      - NODE_ENV=production